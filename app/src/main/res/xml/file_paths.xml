<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the update staging directory is ever exposed, never the database or attachments. -->
    <files-path name="updates" path="updates/" />
</paths>
