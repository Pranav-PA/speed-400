# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
app/build/

# Never commit signing material or the Gemini key (§14)
*.jks
*.keystore
keystore.properties
secrets.properties

# Signing material — see docs/releasing.md. Losing these is bad; committing them is worse.
release.jks
*.jks
keystore.properties
dist/
.kotlin/

# Signing by-products — as sensitive as the keystore itself.
keystore-base64.txt
